<x-layouts.app title="{{ $experiment->name }}">
    <x-page-header title="{{ $experiment->name }}" description="Repeatable GA performance results created by {{ $experiment->creator->name }}."><a href="{{ route('experiments.index') }}" class="rounded-lg border border-slate-300 px-4 py-2.5 text-sm font-semibold text-slate-700">Back to performance tests</a></x-page-header>
    <div class="grid gap-4 sm:grid-cols-2 xl:grid-cols-5">@foreach([['Success rate', number_format($experiment->success_rate, 1).'%'], ['Average fitness', number_format($experiment->average_fitness, 2)], ['Best fitness', number_format($experiment->best_fitness, 2)], ['Avg utilization', number_format($experiment->average_utilization, 1).'%'], ['Avg runtime', number_format($experiment->average_execution_ms, 1).' ms']] as [$label, $value])<div class="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><p class="text-sm text-slate-500">{{ $label }}</p><p class="mt-2 text-2xl font-bold text-slate-900">{{ $value }}</p></div>@endforeach</div>
    <div class="mt-6 grid gap-6 lg:grid-cols-3"><section class="rounded-2xl border border-slate-200 bg-white p-6 shadow-sm"><h2 class="font-semibold text-slate-900">Controlled parameters</h2><dl class="mt-4 space-y-3 text-sm">@foreach([['Repetitions', $experiment->repetitions], ['Population', $experiment->population_size], ['Generations', $experiment->generations], ['Mutation rate', $experiment->mutation_rate], ['Base seed', $experiment->base_seed], ['Events', $experiment->events_count]] as [$label, $value])<div class="flex justify-between"><dt class="text-slate-500">{{ $label }}</dt><dd class="font-semibold text-slate-800">{{ $value }}</dd></div>@endforeach</dl></section><section class="overflow-hidden rounded-2xl border border-slate-200 bg-white shadow-sm lg:col-span-2"><div class="border-b border-slate-100 p-6"><h2 class="font-semibold text-slate-900">Individual trials</h2></div><div class="overflow-x-auto"><table class="w-full text-left text-sm"><thead class="bg-slate-50 text-xs uppercase text-slate-500"><tr><th class="px-5 py-3">Trial</th><th class="px-5 py-3">Seed</th><th class="px-5 py-3">Fitness</th><th class="px-5 py-3">Conflicts</th><th class="px-5 py-3">Utilization</th><th class="px-5 py-3">Runtime</th></tr></thead><tbody class="divide-y divide-slate-100">@foreach($experiment->results as $result)<tr><td class="px-5 py-3">{{ $result['iteration'] }}</td><td class="px-5 py-3 font-mono text-xs">{{ $result['seed'] }}</td><td class="px-5 py-3">{{ number_format($result['fitness'], 2) }}</td><td class="px-5 py-3">{{ $result['hard_conflicts'] }}</td><td class="px-5 py-3">{{ number_format($result['utilization_percent'], 1) }}%</td><td class="px-5 py-3">{{ $result['execution_ms'] }} ms</td></tr>@endforeach</tbody></table></div></section></div>
</x-layouts.app>
