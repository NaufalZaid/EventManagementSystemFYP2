<x-layouts.app title="{{ $event->title }}">
    @php($schedule = $event->schedules->first())
    @php($remaining = max(0, $event->capacity - $event->registered_count))
    <div class="mb-5"><a href="{{ route('discover.index') }}" class="text-sm font-semibold text-indigo-600">← Back to event discovery</a></div>
    <div class="grid gap-6 lg:grid-cols-3"><article class="rounded-2xl border border-slate-200 bg-white p-7 shadow-sm lg:col-span-2"><div class="flex flex-wrap items-center gap-3"><span class="rounded-full bg-indigo-50 px-2.5 py-1 text-xs font-semibold text-indigo-700">{{ str($event->event_type)->headline() }}</span>@if($event->committee)<span class="text-sm text-slate-500">{{ $event->committee }}</span>@endif</div><h1 class="mt-4 text-3xl font-bold tracking-tight text-slate-900">{{ $event->title }}</h1><p class="mt-2 text-sm text-slate-500">Organized by {{ $event->organizer?->name ?? 'MMU' }}</p><div class="mt-7 whitespace-pre-line text-sm leading-7 text-slate-700">{{ $event->description ?: 'No description provided.' }}</div></article>
    <aside class="rounded-2xl border border-slate-200 bg-white p-6 shadow-sm"><h2 class="font-semibold text-slate-900">Event schedule</h2><dl class="mt-5 space-y-4 text-sm"><div><dt class="text-xs text-slate-500">Date</dt><dd class="mt-1 font-semibold text-slate-800">{{ $schedule->timeslot->slot_date->format('l, d F Y') }}</dd></div><div><dt class="text-xs text-slate-500">Time</dt><dd class="mt-1 font-semibold text-slate-800">{{ substr($schedule->timeslot->start_time, 0, 5) }}–{{ substr($schedule->timeslot->end_time, 0, 5) }}</dd></div><div><dt class="text-xs text-slate-500">Venue</dt><dd class="mt-1 font-semibold text-slate-800">{{ $schedule->venue->name }}@if($schedule->venue->location)<br><span class="font-normal text-slate-500">{{ $schedule->venue->location }}</span>@endif</dd></div><div><dt class="text-xs text-slate-500">Availability</dt><dd class="mt-1 font-semibold {{ $remaining ? 'text-emerald-600' : 'text-red-600' }}">{{ $remaining }} of {{ $event->capacity }} places remaining</dd></div></dl><div class="mt-6 border-t border-slate-100 pt-5">@if($isRegistered)<form action="{{ route('events.registration.destroy', $event) }}" method="POST">@csrf @method('DELETE')<button onclick="return confirm('Cancel your registration for this event?')" class="w-full rounded-lg border border-red-300 px-4 py-2.5 text-sm font-semibold text-red-600 hover:bg-red-50">Cancel registration</button></form>@elseif($remaining)<form action="{{ route('events.register', $event) }}" method="POST">@csrf<button class="w-full rounded-lg bg-indigo-600 px-4 py-2.5 text-sm font-semibold text-white hover:bg-indigo-700">Register now</button></form>@else<span class="block rounded-lg bg-slate-100 px-4 py-2.5 text-center text-sm font-semibold text-slate-500">Registration full</span>@endif</div></aside></div>
</x-layouts.app>
