<x-layouts.app title="Event analytics">
    <x-page-header title="Event analytics" description="Compare registration demand, attendance, and scheduled venue use." />
    @php($overallRate = $totalRegistrations ? round($totalAttendance / $totalRegistrations * 100) : 0)
    <div class="mb-6 grid gap-4 sm:grid-cols-3"><div class="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><p class="text-sm text-slate-500">Events tracked</p><p class="mt-2 text-3xl font-bold text-slate-900">{{ $events->count() }}</p></div><div class="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><p class="text-sm text-slate-500">Active registrations</p><p class="mt-2 text-3xl font-bold text-indigo-600">{{ $totalRegistrations }}</p></div><div class="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><p class="text-sm text-slate-500">Overall attendance</p><p class="mt-2 text-3xl font-bold text-emerald-600">{{ $overallRate }}%</p></div></div>
    <section class="overflow-hidden rounded-2xl border border-slate-200 bg-white shadow-sm"><div class="border-b border-slate-100 p-6"><h2 class="font-semibold text-slate-900">Event performance</h2></div>@if($events->isEmpty())<p class="p-6 text-sm text-slate-500">No scheduled events to analyse.</p>@else<div class="overflow-x-auto"><table class="w-full text-left text-sm"><thead class="bg-slate-50 text-xs uppercase text-slate-500"><tr><th class="px-6 py-4">Event</th><th class="px-6 py-4">Venue</th><th class="px-6 py-4">Registrations</th><th class="px-6 py-4">Attendance</th><th class="px-6 py-4">Rate</th><th class="px-6 py-4"></th></tr></thead><tbody class="divide-y divide-slate-100">@foreach($events as $event)@php($rate = $event->registered_count ? round($event->attended_count / $event->registered_count * 100) : 0)<tr><td class="px-6 py-4"><p class="font-semibold text-slate-900">{{ $event->title }}</p><p class="mt-1 text-xs text-slate-500">{{ $event->organizer?->name }}</p></td><td class="px-6 py-4">{{ $event->schedules->first()?->venue->name }}</td><td class="px-6 py-4">{{ $event->registered_count }} / {{ $event->capacity }}</td><td class="px-6 py-4">{{ $event->attended_count }}</td><td class="px-6 py-4"><span class="font-semibold {{ $rate >= 70 ? 'text-emerald-600' : ($rate ? 'text-amber-600' : 'text-slate-400') }}">{{ $rate }}%</span></td><td class="px-6 py-4 text-right"><a href="{{ route('events.attendance.show', $event) }}" class="text-xs font-semibold text-indigo-600">Manage →</a></td></tr>@endforeach</tbody></table></div>@endif</section>
    @if(auth()->user()->hasRole('administrator') && $venues->isNotEmpty())<section class="mt-6 rounded-2xl border border-slate-200 bg-white p-6 shadow-sm"><h2 class="font-semibold text-slate-900">Venue scheduling usage</h2><div class="mt-5 grid gap-3 sm:grid-cols-2 lg:grid-cols-4">@foreach($venues as $venue)<div class="rounded-xl bg-slate-50 p-4"><p class="text-sm font-semibold text-slate-800">{{ $venue->name }}</p><p class="mt-2 text-2xl font-bold text-indigo-600">{{ $venue->schedules_count }}</p><p class="text-xs text-slate-500">scheduled {{ Str::plural('event', $venue->schedules_count) }}</p></div>@endforeach</div></section>@endif
</x-layouts.app>
