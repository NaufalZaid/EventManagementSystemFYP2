<x-layouts.app title="Venue blackouts">
    <x-page-header title="{{ $venue->name }} blackouts" description="Block maintenance, examinations, or other unavailable periods." />
    <div class="grid gap-6 lg:grid-cols-5"><section class="rounded-2xl border border-slate-200 bg-white p-6 shadow-sm lg:col-span-2"><h2 class="font-semibold text-slate-900">Add blackout</h2><x-form-errors /><form action="{{ route('venues.blackouts.store', $venue) }}" method="POST" class="mt-5 space-y-4">@csrf<div><label class="mb-2 block text-sm font-medium">Starts</label><input type="datetime-local" name="starts_at" value="{{ old('starts_at') }}" required class="block w-full rounded-lg border border-slate-300 p-2.5 text-sm"></div><div><label class="mb-2 block text-sm font-medium">Ends</label><input type="datetime-local" name="ends_at" value="{{ old('ends_at') }}" required class="block w-full rounded-lg border border-slate-300 p-2.5 text-sm"></div><div><label class="mb-2 block text-sm font-medium">Reason</label><input name="reason" value="{{ old('reason') }}" required maxlength="255" class="block w-full rounded-lg border border-slate-300 p-2.5 text-sm" placeholder="Maintenance"></div><button class="w-full rounded-lg bg-indigo-600 px-4 py-2.5 text-sm font-semibold text-white">Add blackout</button></form></section>
    <section class="rounded-2xl border border-slate-200 bg-white p-6 shadow-sm lg:col-span-3"><h2 class="font-semibold text-slate-900">Unavailable periods</h2>@if($blackouts->isEmpty())<p class="mt-4 text-sm text-slate-500">No blackout periods recorded.</p>@else<div class="mt-4 divide-y divide-slate-100">@foreach($blackouts as $blackout)<div class="flex items-center justify-between gap-4 py-4"><div><p class="text-sm font-semibold text-slate-800">{{ $blackout->reason }}</p><p class="mt-1 text-xs text-slate-500">{{ $blackout->starts_at->format('d M Y, H:i') }} – {{ $blackout->ends_at->format('d M Y, H:i') }}</p></div><form action="{{ route('venues.blackouts.destroy', [$venue, $blackout]) }}" method="POST">@csrf @method('DELETE')<button class="text-xs font-semibold text-red-600">Remove</button></form></div>@endforeach</div>@endif</section></div>
</x-layouts.app>
