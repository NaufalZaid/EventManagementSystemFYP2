<x-layouts.app title="GA Optimizer">
    <x-page-header title="Genetic Algorithm optimizer" description="Generate venue and timeslot assignments for approved events while checking scheduling constraints."><a href="{{ route('optimizer.comparison') }}" class="rounded-lg border border-purple-300 px-4 py-2.5 text-sm font-semibold text-purple-700">Compare schedules</a></x-page-header>
    <x-form-errors />
        <div class="grid gap-6 xl:grid-cols-5"><section class="rounded-2xl border border-purple-200 bg-purple-50 p-6 xl:col-span-2"><span class="text-xs font-semibold uppercase tracking-wider text-purple-700">New optimization run</span><h2 class="mt-2 text-xl font-bold text-slate-900">Generate candidate schedule</h2><div class="mt-5 grid grid-cols-3 gap-3 text-center"><div class="rounded-xl bg-white p-3"><p class="text-2xl font-bold text-slate-900">{{ $eligibleCount }}</p><p class="text-xs text-slate-500">Events</p></div><div class="rounded-xl bg-white p-3"><p class="text-2xl font-bold text-slate-900">{{ $venueCount }}</p><p class="text-xs text-slate-500">Venues</p></div><div class="rounded-xl bg-white p-3"><p class="text-2xl font-bold text-slate-900">{{ $timeslotCount }}</p><p class="text-xs text-slate-500">Slots</p></div></div><form action="{{ route('optimizer.store') }}" method="POST" class="mt-5 space-y-4">@csrf<div><label class="mb-2 block text-sm font-medium text-slate-700">Population size</label><input name="population_size" type="number" min="10" max="300" value="{{ old('population_size', 60) }}" required class="block w-full rounded-lg border border-purple-200 p-2.5 text-sm"><p class="mt-1 text-xs text-slate-500">Candidate schedules per generation.</p></div><div><label class="mb-2 block text-sm font-medium text-slate-700">Generations</label><input name="generations" type="number" min="5" max="1000" value="{{ old('generations', 150) }}" required class="block w-full rounded-lg border border-purple-200 p-2.5 text-sm"></div><div><label class="mb-2 block text-sm font-medium text-slate-700">Mutation rate</label><input name="mutation_rate" type="number" min="0.01" max="0.5" step="0.01" value="{{ old('mutation_rate', 0.08) }}" required class="block w-full rounded-lg border border-purple-200 p-2.5 text-sm"></div><div><label class="mb-2 block text-sm font-medium text-slate-700">Random seed <span class="font-normal text-slate-500">(optional)</span></label><input name="seed" type="number" min="1" value="{{ old('seed') }}" class="block w-full rounded-lg border border-purple-200 p-2.5 text-sm" placeholder="Generated automatically"><p class="mt-1 text-xs text-slate-500">Reuse a seed to reproduce the same evolutionary choices.</p></div><button @disabled(!$eligibleCount || !$venueCount || !$timeslotCount) class="w-full rounded-lg bg-purple-600 px-4 py-3 text-sm font-semibold text-white disabled:cursor-not-allowed disabled:bg-slate-300">Run optimizer</button></form><p class="mt-4 text-xs leading-5 text-slate-500">Only approved, unscheduled events are included. Review and explicitly apply a conflict-free result before it becomes part of the event schedule.</p></section>
    <section class="overflow-hidden rounded-2xl border border-slate-200 bg-white shadow-sm xl:col-span-3"><div class="border-b border-slate-100 p-6"><h2 class="font-semibold text-slate-900">Optimization history</h2><p class="mt-1 text-sm text-slate-500">Stored parameters, fitness, conflicts, and execution time.</p></div>@if($runs->isEmpty())<p class="p-8 text-center text-sm text-slate-500">No optimizer runs yet.</p>@else<div class="divide-y divide-slate-100">@foreach($runs as $run)<a href="{{ route('optimizer.show', $run) }}" class="flex flex-wrap items-center justify-between gap-4 px-6 py-5 hover:bg-slate-50"><div><div class="flex items-center gap-2"><p class="font-semibold text-slate-900">Run #{{ $run->id }}</p><span class="rounded-full px-2 py-0.5 text-xs font-semibold {{ $run->hard_conflicts ? 'bg-red-50 text-red-700' : 'bg-emerald-50 text-emerald-700' }}">{{ $run->hard_conflicts }} conflicts</span>@if($run->applied_at)<span class="rounded-full bg-purple-50 px-2 py-0.5 text-xs font-semibold text-purple-700">Applied</span>@endif</div><p class="mt-1 text-xs text-slate-500">{{ $run->events_count }} events · population {{ $run->population_size }} · {{ $run->generations }} generations · {{ $run->execution_ms }} ms</p></div><div class="text-right"><p class="text-lg font-bold text-purple-700">{{ number_format($run->best_fitness, 2) }}</p><p class="text-xs text-slate-500">fitness</p></div></a>@endforeach</div>@endif</section></div>
</x-layouts.app>
