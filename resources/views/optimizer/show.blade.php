<x-layouts.app title="Optimization run #{{ $run->id }}">
    <x-page-header title="Optimization run #{{ $run->id }}" description="Generated by {{ $run->creator->name }} on {{ $run->created_at->format('d M Y, H:i') }} · seed {{ $run->metrics['seed'] ?? 'legacy' }}."><a href="{{ route('optimizer.index') }}" class="rounded-lg border border-slate-300 px-4 py-2.5 text-sm font-semibold text-slate-700">Back to optimizer</a></x-page-header>
    <div class="mb-6 grid gap-4 sm:grid-cols-2 xl:grid-cols-5">@foreach([['Fitness', number_format($run->best_fitness, 2)], ['Hard conflicts', $run->hard_conflicts], ['Utilization', number_format($run->utilization_percent, 1).'%'], ['Soft penalty', number_format($run->metrics['soft_penalty'] ?? 0, 2)], ['Runtime', $run->execution_ms.' ms']] as [$label, $value])<div class="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><p class="text-sm text-slate-500">{{ $label }}</p><p class="mt-2 text-2xl font-bold text-slate-900">{{ $value }}</p></div>@endforeach</div>
    @if($run->hard_conflicts)<div class="mb-6 rounded-xl border border-red-200 bg-red-50 p-4 text-sm text-red-700">This run cannot be applied. Increase the available venues/timeslots or run a larger population to resolve all hard conflicts.</div>@elseif(!$run->applied_at)<div class="mb-6 flex flex-wrap items-center justify-between gap-4 rounded-xl border border-emerald-200 bg-emerald-50 p-4"><p class="text-sm font-medium text-emerald-800">This result is conflict-free and ready for final validation.</p><form action="{{ route('optimizer.apply', $run) }}" method="POST">@csrf<button onclick="return confirm('Apply all generated assignments to the live schedule?')" class="rounded-lg bg-emerald-600 px-4 py-2.5 text-sm font-semibold text-white">Apply generated schedule</button></form></div>@else<div class="mb-6 rounded-xl border border-purple-200 bg-purple-50 p-4 text-sm font-semibold text-purple-700">Applied on {{ $run->applied_at->format('d M Y, H:i') }}.</div>@endif
    <div class="overflow-hidden rounded-2xl border border-slate-200 bg-white shadow-sm"><div class="overflow-x-auto"><table class="w-full text-left text-sm"><thead class="bg-slate-50 text-xs uppercase text-slate-500"><tr><th class="px-6 py-4">Event</th><th class="px-6 py-4">Venue</th><th class="px-6 py-4">Timeslot</th><th class="px-6 py-4">Preference results</th><th class="px-6 py-4">Penalty</th></tr></thead><tbody class="divide-y divide-slate-100">@foreach($run->assignments as $assignment)<tr class="align-top"><td class="px-6 py-4"><p class="font-semibold text-slate-900">{{ $assignment->event->title }}</p><p class="mt-1 text-xs text-slate-500">{{ $assignment->event->capacity }} people · {{ $assignment->event->duration_minutes }} min</p></td><td class="px-6 py-4">{{ $assignment->venue?->name ?? 'Unassigned' }}@if($assignment->venue)<p class="mt-1 text-xs text-slate-500">{{ $assignment->venue->capacity }} seats</p>@endif</td><td class="px-6 py-4">@if($assignment->timeslot){{ $assignment->timeslot->slot_date->format('d M Y') }}<p class="mt-1 text-xs text-slate-500">{{ substr($assignment->timeslot->start_time, 0, 5) }}–{{ substr($assignment->timeslot->end_time, 0, 5) }}</p>@else<span class="text-red-600">No valid option</span>@endif</td><td class="px-6 py-4"><div class="flex flex-wrap gap-1">@foreach(['preferred_venue_met' => 'Venue', 'preferred_date_met' => 'Date', 'preferred_time_met' => 'Time'] as $key => $label)@if(array_key_exists($key, $assignment->details ?? []))<span class="rounded-full px-2 py-0.5 text-xs font-semibold {{ $assignment->details[$key] ? 'bg-emerald-50 text-emerald-700' : 'bg-amber-50 text-amber-700' }}">{{ $label }} {{ $assignment->details[$key] ? 'met' : 'missed' }}</span>@endif @endforeach @if(($assignment->details['unassigned'] ?? false))<span class="text-xs font-semibold text-red-600">Unassigned</span>@endif</div></td><td class="px-6 py-4 font-semibold text-slate-700">{{ number_format($assignment->soft_penalty, 2) }}</td></tr>@endforeach</tbody></table></div></div>
</x-layouts.app>
