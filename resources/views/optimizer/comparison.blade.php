<x-layouts.app title="Schedule comparison">
    <x-page-header title="Manual vs generated schedules" description="Compare applied schedule sources using conflict and venue-capacity metrics."><a href="{{ route('optimizer.index') }}" class="rounded-lg bg-purple-600 px-4 py-2.5 text-sm font-semibold text-white">Open optimizer</a></x-page-header>
    <div class="grid gap-6 lg:grid-cols-2">@foreach([['Manual assignments', $manual, false], ['GA-generated assignments', $generated, true]] as [$title, $metrics, $generatedSource])<section class="rounded-2xl border border-slate-200 bg-white p-6 shadow-sm"><div class="flex items-center justify-between"><h2 class="font-semibold text-slate-900">{{ $title }}</h2><span class="rounded-full px-2.5 py-1 text-xs font-semibold {{ $generatedSource ? 'bg-purple-50 text-purple-700' : 'bg-blue-50 text-blue-700' }}">{{ $metrics['count'] }} assignments</span></div><dl class="mt-6 grid grid-cols-3 gap-4 text-center"><div class="rounded-xl bg-slate-50 p-4"><dt class="text-xs text-slate-500">Conflicts</dt><dd class="mt-2 text-2xl font-bold text-slate-900">{{ $metrics['conflicts'] }}</dd></div><div class="rounded-xl bg-slate-50 p-4"><dt class="text-xs text-slate-500">Utilization</dt><dd class="mt-2 text-2xl font-bold text-slate-900">{{ number_format($metrics['utilization'], 1) }}%</dd></div><div class="rounded-xl bg-slate-50 p-4"><dt class="text-xs text-slate-500">Unused seats</dt><dd class="mt-2 text-2xl font-bold text-slate-900">{{ $metrics['unused_seats'] }}</dd></div></dl></section>@endforeach</div>
    <section class="mt-6 rounded-2xl border border-slate-200 bg-white p-6 shadow-sm"><h2 class="font-semibold text-slate-900">Recent GA evidence</h2><p class="mt-1 text-sm text-slate-500">Execution time and best-candidate quality across runs.</p>@if($runs->isEmpty())<p class="mt-5 text-sm text-slate-500">No optimization evidence yet.</p>@else<div class="mt-5 overflow-x-auto"><table class="w-full text-left text-sm"><thead class="text-xs uppercase text-slate-500"><tr><th class="py-3">Run</th><th>Events</th><th>Fitness</th><th>Conflicts</th><th>Utilization</th><th>Runtime</th></tr></thead><tbody class="divide-y divide-slate-100">@foreach($runs as $run)<tr><td class="py-3"><a href="{{ route('optimizer.show', $run) }}" class="font-semibold text-purple-600">#{{ $run->id }}</a></td><td>{{ $run->events_count }}</td><td>{{ number_format($run->best_fitness, 2) }}</td><td>{{ $run->hard_conflicts }}</td><td>{{ number_format($run->utilization_percent, 1) }}%</td><td>{{ $run->execution_ms }} ms</td></tr>@endforeach</tbody></table></div>@endif</section>
</x-layouts.app>
