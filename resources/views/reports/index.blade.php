<x-layouts.app title="Reports">
    <x-page-header title="Operational reports" description="Filter event performance and venue use for report-ready evidence." />
    <form method="GET" action="{{ route('reports.index') }}" class="mb-6 flex flex-wrap items-end gap-3 rounded-2xl border border-slate-200 bg-white p-4 shadow-sm"><div><label class="mb-2 block text-xs font-semibold text-slate-500">From</label><input name="from" type="date" value="{{ $from }}" class="rounded-lg border border-slate-300 p-2.5 text-sm"></div><div><label class="mb-2 block text-xs font-semibold text-slate-500">To</label><input name="to" type="date" value="{{ $to }}" class="rounded-lg border border-slate-300 p-2.5 text-sm"></div><button class="rounded-lg bg-indigo-600 px-4 py-2.5 text-sm font-semibold text-white">Apply dates</button><div class="ml-auto flex gap-2"><a href="{{ route('reports.events.csv', ['from' => $from, 'to' => $to]) }}" class="rounded-lg border border-slate-300 px-3 py-2.5 text-xs font-semibold text-slate-700">Event CSV</a><a href="{{ route('reports.venues.csv', ['from' => $from, 'to' => $to]) }}" class="rounded-lg border border-slate-300 px-3 py-2.5 text-xs font-semibold text-slate-700">Venue CSV</a></div></form>
    <section class="overflow-hidden rounded-2xl border border-slate-200 bg-white shadow-sm"><div class="border-b border-slate-100 p-6"><h2 class="font-semibold text-slate-900">Event performance</h2><p class="mt-1 text-sm text-slate-500">{{ $events->count() }} scheduled event(s) in the selected period.</p></div><div class="overflow-x-auto"><table class="w-full text-left text-sm"><thead class="bg-slate-50 text-xs uppercase text-slate-500"><tr><th class="px-5 py-3">Event</th><th class="px-5 py-3">Date</th><th class="px-5 py-3">Venue</th><th class="px-5 py-3">Registered</th><th class="px-5 py-3">Attended</th><th class="px-5 py-3">Rate</th></tr></thead><tbody class="divide-y divide-slate-100">@forelse($events as $event)@php($schedule=$event->schedules->first())@php($rate=$event->registered_count ? round($event->attended_count/$event->registered_count*100) : 0)<tr><td class="px-5 py-3"><p class="font-semibold text-slate-900">{{ $event->title }}</p><p class="text-xs text-slate-500">{{ $event->organizer?->name }}</p></td><td class="px-5 py-3">{{ $schedule?->timeslot->slot_date->format('d M Y') }}</td><td class="px-5 py-3">{{ $schedule?->venue->name }}</td><td class="px-5 py-3">{{ $event->registered_count }}</td><td class="px-5 py-3">{{ $event->attended_count }}</td><td class="px-5 py-3 font-semibold">{{ $rate }}%</td></tr>@empty<tr><td colspan="6" class="px-5 py-8 text-center text-slate-500">No events in this date range.</td></tr>@endforelse</tbody></table></div></section>
    <section class="mt-6 rounded-2xl border border-slate-200 bg-white p-6 shadow-sm"><h2 class="font-semibold text-slate-900">Venue utilization</h2><div class="mt-5 grid gap-4 sm:grid-cols-2 xl:grid-cols-4">@forelse($venues as $venue)<div class="rounded-xl bg-slate-50 p-4"><p class="font-semibold text-slate-800">{{ $venue['name'] }}</p><p class="mt-3 text-2xl font-bold text-indigo-600">{{ number_format($venue['capacity_utilization'], 1) }}%</p><p class="text-xs text-slate-500">capacity utilization</p><p class="mt-3 text-xs text-slate-500">{{ $venue['event_count'] }} events · {{ $venue['occupied_minutes'] }} occupied minutes</p></div>@empty<p class="text-sm text-slate-500">No venue usage in this period.</p>@endforelse</div></section>
</x-layouts.app>
