<x-layouts.app title="Evaluate the system">
    <x-page-header title="System usability evaluation" description="Provide structured feedback for the FYP evaluation. You can update your response later." />
    <div class="mx-auto max-w-3xl rounded-2xl border border-slate-200 bg-white p-7 shadow-sm"><x-form-errors /><form action="{{ route('evaluation.update') }}" method="POST" class="space-y-7">@csrf @method('PUT')
        @foreach(['ease_of_use' => 'The system is easy to learn and use.', 'usefulness' => 'The system is useful for university event management.', 'scheduling_confidence' => 'The scheduling and conflict information increases my confidence.', 'satisfaction' => 'Overall, I am satisfied with the system.'] as $field => $question)<fieldset><legend class="text-sm font-semibold text-slate-800">{{ $question }}</legend><div class="mt-3 grid grid-cols-5 gap-2">@foreach(range(1,5) as $score)<label class="cursor-pointer rounded-lg border border-slate-200 p-3 text-center hover:bg-indigo-50"><input type="radio" name="{{ $field }}" value="{{ $score }}" required @checked((int) old($field, $evaluation?->{$field}) === $score) class="text-indigo-600"><span class="mt-1 block text-sm font-semibold text-slate-700">{{ $score }}</span><span class="block text-[10px] text-slate-400">{{ $score === 1 ? 'Strongly disagree' : ($score === 5 ? 'Strongly agree' : '') }}</span></label>@endforeach</div></fieldset>@endforeach
        <div><label for="comments" class="mb-2 block text-sm font-semibold text-slate-800">Additional comments</label><textarea id="comments" name="comments" rows="5" maxlength="3000" class="block w-full rounded-lg border border-slate-300 p-2.5 text-sm" placeholder="What worked well, and what should be improved?">{{ old('comments', $evaluation?->comments) }}</textarea></div>
        <label class="flex items-start gap-3 rounded-xl border border-blue-200 bg-blue-50 p-4"><input type="checkbox" name="consent" value="1" required @checked(old('consent', $evaluation?->consent)) class="mt-1 rounded border-blue-300 text-indigo-600"><span class="text-sm leading-6 text-blue-800">I consent to this response being used as aggregated, anonymized evaluation evidence for this academic project.</span></label>
        <button class="w-full rounded-lg bg-indigo-600 px-4 py-3 text-sm font-semibold text-white">{{ $evaluation ? 'Update evaluation' : 'Submit evaluation' }}</button>
    </form></div>
</x-layouts.app>
