<x-layouts.app title="Confirm attendance">
    @php($schedule = $session->event->schedules->first())
    <div class="mx-auto max-w-xl"><x-page-header title="Confirm attendance" description="Secure QR check-in for {{ $session->event->title }}." /><div class="rounded-2xl border border-slate-200 bg-white p-7 shadow-sm"><x-form-errors /><div class="rounded-xl bg-indigo-50 p-5"><h2 class="font-semibold text-slate-900">{{ $session->event->title }}</h2><p class="mt-2 text-sm text-slate-600">{{ $schedule->timeslot->slot_date->format('d M Y') }} · {{ substr($schedule->timeslot->start_time, 0, 5) }}–{{ substr($schedule->timeslot->end_time, 0, 5) }}</p><p class="mt-1 text-sm text-slate-600">{{ $schedule->venue->name }}</p></div>@if(!$registration)<p class="mt-5 rounded-xl border border-red-200 bg-red-50 p-4 text-sm text-red-700">You do not have an active registration for this event.</p>@elseif($alreadyCheckedIn)<div class="mt-5 rounded-xl border border-emerald-200 bg-emerald-50 p-4 text-sm font-semibold text-emerald-700">Your attendance has already been recorded.</div><a href="{{ route('attendance.history') }}" class="mt-4 block text-center text-sm font-semibold text-indigo-600">View attendance history →</a>@else<form action="{{ route('attendance.check-in.store', $token) }}" method="POST" class="mt-5">@csrf<button class="w-full rounded-lg bg-emerald-600 px-4 py-3 text-sm font-semibold text-white hover:bg-emerald-700">Confirm my attendance</button></form><p class="mt-3 text-center text-xs text-slate-500">This records your account, event, time, and QR session.</p>@endif</div></div>
</x-layouts.app>
