<x-layouts.app title="Attendance history">
    <x-page-header title="Attendance history" description="A record of events where your check-in was confirmed." />
    @if($records->isEmpty())<x-empty-state title="No attendance recorded" description="Your successfully confirmed event check-ins will appear here." />@else<div class="space-y-4">@foreach($records as $record)@php($schedule = $record->event->schedules->first())<article class="flex flex-wrap items-center justify-between gap-5 rounded-2xl border border-slate-200 bg-white p-6 shadow-sm"><div><span class="rounded-full bg-emerald-50 px-2.5 py-1 text-xs font-semibold text-emerald-700">Attended</span><h2 class="mt-3 text-lg font-semibold text-slate-900">{{ $record->event->title }}</h2><p class="mt-2 text-sm text-slate-500">{{ $schedule?->timeslot->slot_date->format('d M Y') }} · {{ $schedule?->venue->name }}</p></div><div class="text-right"><p class="text-sm font-semibold text-slate-800">Checked in {{ $record->checked_in_at->format('H:i') }}</p><p class="mt-1 text-xs text-slate-500">Method: {{ strtoupper($record->method) }}</p></div></article>@endforeach</div>@endif
</x-layouts.app>
