<x-layouts.app title="My Events">
    <x-page-header title="My Events" description="Your confirmed registrations, ordered by event schedule."><a href="{{ route('discover.index') }}" class="rounded-lg bg-indigo-600 px-4 py-2.5 text-sm font-semibold text-white">Discover events</a></x-page-header>
    @if($registrations->isEmpty())<x-empty-state title="No registered events" description="Browse published events and register to build your personal event list."><a href="{{ route('discover.index') }}" class="text-sm font-semibold text-indigo-600">Discover events →</a></x-empty-state>@else<div class="space-y-4">@foreach($registrations as $registration)@php($schedule = $registration->event->schedules->first())<article class="flex flex-wrap items-center justify-between gap-5 rounded-2xl border border-slate-200 bg-white p-6 shadow-sm"><div><div class="flex items-center gap-2"><span class="rounded-full bg-emerald-50 px-2.5 py-1 text-xs font-semibold text-emerald-700">Confirmed</span><span class="text-xs text-slate-500">Registered {{ $registration->registered_at->diffForHumans() }}</span></div><h2 class="mt-3 text-lg font-semibold text-slate-900">{{ $registration->event->title }}</h2><p class="mt-2 text-sm text-slate-500">{{ $schedule->timeslot->slot_date->format('d M Y') }} · {{ substr($schedule->timeslot->start_time, 0, 5) }}–{{ substr($schedule->timeslot->end_time, 0, 5) }} · {{ $schedule->venue->name }}</p></div><div class="flex gap-2"><a href="{{ route('discover.show', $registration->event) }}" class="rounded-lg border border-slate-300 px-3.5 py-2 text-xs font-semibold text-slate-700">Details</a><form action="{{ route('events.registration.destroy', $registration->event) }}" method="POST">@csrf @method('DELETE')<button onclick="return confirm('Cancel this registration?')" class="rounded-lg border border-red-300 px-3.5 py-2 text-xs font-semibold text-red-600">Cancel</button></form></div></article>@endforeach</div>@endif
</x-layouts.app>
