<x-layouts.app title="Notifications">
    <x-page-header title="Notifications" description="Announcements and scheduled reminders for your registered events.">@if(auth()->user()->unreadNotifications()->exists())<form action="{{ route('notifications.read-all') }}" method="POST">@csrf @method('PATCH')<button class="rounded-lg border border-slate-300 px-4 py-2.5 text-sm font-semibold text-slate-700">Mark all as read</button></form>@endif</x-page-header>
    @if($notifications->isEmpty())<x-empty-state title="No notifications" description="Event announcements and reminders will appear here." />@else<div class="overflow-hidden rounded-2xl border border-slate-200 bg-white shadow-sm"><div class="divide-y divide-slate-100">@foreach($notifications as $notification)<a href="{{ route('notifications.read', $notification->id) }}" class="flex gap-4 p-5 hover:bg-slate-50 {{ $notification->read_at ? '' : 'bg-indigo-50/60' }}"><span class="mt-1 h-2.5 w-2.5 shrink-0 rounded-full {{ $notification->read_at ? 'bg-slate-300' : 'bg-indigo-600' }}"></span><div class="min-w-0 flex-1"><div class="flex flex-wrap items-center justify-between gap-2"><p class="font-semibold text-slate-900">{{ $notification->data['title'] ?? 'Event update' }}</p><time class="text-xs text-slate-400">{{ $notification->created_at->diffForHumans() }}</time></div><p class="mt-1 text-xs font-medium text-indigo-600">{{ $notification->data['event_title'] ?? 'Event Management System' }}</p><p class="mt-2 text-sm leading-6 text-slate-600">{{ $notification->data['message'] ?? '' }}</p></div></a>@endforeach</div></div><div class="mt-5">{{ $notifications->links() }}</div>@endif
</x-layouts.app>
